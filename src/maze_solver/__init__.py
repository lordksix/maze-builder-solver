"""Package that handles the serialization and file creation.

Subpackages exported by this package:

- `graph`: Package that creates and handles the graph made from the maze
    instance and solve it.
- `models`: Package that creates and handles the models for the creation
    of the maze.
- `persistence`: Package that handles the serialization and file creation.
- `views`: Package that handles the visualization of the maze.
"""
